import { OpeningComponentPremises } from './opening-component-premises';

describe('OpeningComponentPremises', () => {
  it('should create an instance', () => {
    expect(new OpeningComponentPremises()).toBeTruthy();
  });
});
