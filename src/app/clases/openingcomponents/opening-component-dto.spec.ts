import { OpeningComponentDTO } from './opening-component-dto';

describe('OpeningComponentDTO', () => {
  it('should create an instance', () => {
    expect(new OpeningComponentDTO()).toBeTruthy();
  });
});
